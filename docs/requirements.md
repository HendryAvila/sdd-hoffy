> ⚡ Auto-generated by sdd_reverse_engineer — review and refine as needed

# Hoofy — Requirements

> Generated by [SDD-Hoffy](https://github.com/HendryAvila/Hoofy) | Stage 2: Specify

## Functional Requirements

### Must Have

- **FR-001**: MCP server MUST communicate via stdio transport using the Model Context Protocol
- **FR-002**: MUST provide persistent memory across sessions using SQLite with FTS5 full-text search
- **FR-003**: MUST support saving, searching, and retrieving observations (decisions, bugs, patterns, discoveries)
- **FR-004**: MUST support session lifecycle management (start, end, summary)
- **FR-005**: MUST implement a 9-stage sequential greenfield pipeline: init → principles → charter → specify → business-rules → clarify → design → tasks → validate
- **FR-006**: MUST enforce a Clarity Gate at the clarify stage that blocks advancement until the clarity score meets the mode threshold (70 for guided, 50 for expert)
- **FR-007**: MUST implement an adaptive change pipeline with 12 flow variants (4 types × 3 sizes) that selects stages automatically
- **FR-008**: MUST support 4 change types: feature, fix, refactor, enhancement
- **FR-009**: MUST support 3 change sizes: small (4 stages), medium (5 stages), large (6-7 stages)
- **FR-010**: MUST include mandatory context-check as the second stage in ALL change flows
- **FR-011**: MUST support Architecture Decision Records (ADRs) stored in docs/adrs/ with sequential NNN-slug.md naming
- **FR-012**: MUST provide a reverse-engineer scanner that reads existing codebases and produces structured reports (read-only)
- **FR-013**: MUST provide a bootstrap tool that writes missing SDD artifacts from AI-generated content
- **FR-014**: MUST support two interaction modes: guided (step-by-step) and expert (streamlined)
- **FR-015**: MUST persist pipeline state in docs/hoofy.json in the user's project directory
- **FR-016**: MUST provide MCP prompts for on-demand guidance (/sdd-start, /sdd-status, /sdd-stage-guide, /sdd-memory-guide, /sdd-change-guide, /sdd-bootstrap-guide)
- **FR-017**: MUST support typed directional relations between observations (depends_on, caused_by, implements, supersedes, relates_to, part_of)
- **FR-018**: MUST support graph traversal via mem_get(depth) to navigate related observations
- **FR-019**: MUST support observation namespaces for sub-agent memory isolation
- **FR-020**: MUST provide a self-update mechanism via GitHub Releases API with binary replacement
- **FR-021**: MUST support principles stage capturing golden invariants, coding standards, and domain truths before requirements
- **FR-022**: MUST support charter stage with enterprise-grade project definition (problem statement, target users, vision, boundaries, stakeholders, constraints)
- **FR-023**: MUST support business rules extraction using BRG taxonomy (definitions, facts, constraints, derivations, glossary) and DDD Ubiquitous Language
- **FR-024**: MUST support sdd_audit tool that compares specs against source code for discrepancies (read-only)
- **FR-025**: MUST auto-generate SDD instructions section in agent files (CLAUDE.md/AGENTS.md) during sdd_init_project
- **FR-026**: MUST support explore tool for pre-pipeline structured context capture with topic key upsert
- **FR-027**: MUST support wave assignments in task breakdowns for parallel execution planning
- **FR-028**: MUST support progress documents for long-running sessions via mem_progress
- **FR-029**: MUST support memory compaction to clean stale observations with optional summary replacement
- **FR-030**: MUST archive completed changes from docs/changes/ to docs/history/

### Should Have

- **FR-031**: SHOULD provide spec-aware code review via sdd_review tool that generates checklists from project specs
- **FR-032**: SHOULD provide ad-hoc context suggestion via sdd_suggest_context for sessions without formal pipelines
- **FR-033**: SHOULD support detail_level parameter (summary/standard/full) across memory and SDD tools for token budget control
- **FR-034**: SHOULD support max_tokens parameter for token budget capping in search and context tools
- **FR-035**: SHOULD support passive learning capture from conversation content via mem_save(save_type="passive")
- **FR-036**: SHOULD support topic_key for upsert semantics (evolving topics update a single observation)
- **FR-037**: SHOULD support suggestion of stable topic keys via mem_suggest_topic_key

### Could Have

- **FR-038**: COULD provide interactive documentation site
- **FR-039**: COULD provide Homebrew tap for macOS installation
- **FR-040**: COULD provide PowerShell install script for Windows
- **FR-041**: COULD support custom clarity dimensions beyond the default 8

### Won't Have (this version)

- **FR-W01**: Will NOT support HTTP/SSE transport — stdio only
- **FR-W02**: Will NOT support multi-user collaboration — single user per project
- **FR-W03**: Will NOT support real-time streaming of pipeline progress
- **FR-W04**: Will NOT support cloud-hosted memory — local SQLite only
- **FR-W05**: Will NOT generate code — tools are storage-only, AI generates content

## Non-Functional Requirements

- **NFR-001**: Binary MUST be statically compiled (CGO_ENABLED=0) for zero-dependency deployment
- **NFR-002**: All builds MUST target linux/darwin/windows on amd64/arm64 (6 platform binaries)
- **NFR-003**: Memory DB MUST use embedded SQLite (modernc.org/sqlite — pure Go, no CGO)
- **NFR-004**: All user-facing messages MUST go to stderr to keep stdout clean for MCP stdio transport
- **NFR-005**: FTS5 search MUST sanitize user input to prevent query injection
- **NFR-006**: All tests MUST run with race detector enabled (-race flag)
- **NFR-007**: Pipeline state transitions MUST be atomic — no partial stage completion
- **NFR-008**: Memory operations MUST be safe for concurrent access (SQLite WAL mode)
- **NFR-009**: Tool handlers MUST wrap errors with fmt.Errorf("context: %w", err) for chain
- **NFR-010**: Server instructions MUST be kept minimal (~160 lines) with on-demand prompt loading to reduce token overhead

## Constraints

- MUST use Go 1.25+ as the implementation language
- MUST use mcp-go v0.44.0 (Mark3Labs MCP SDK) for protocol compliance
- MUST use modernc.org/sqlite for embedded database (no CGO dependency)
- MUST follow conventional commits (feat:, fix:, refactor:, test:, docs:, ci:, chore:)
- MUST use GoReleaser for cross-platform release builds
- Memory DB path defaults to ~/.hoofy/memory.db

## Assumptions

- AI clients support MCP protocol and stdio transport
- Users have a project directory where docs/ can be created
- Users have internet access for self-update functionality
- The AI generates high-quality content — Hoofy tools are storage, not generators
- One active project pipeline and one active change pipeline at a time

## Dependencies

- **mcp-go v0.44.0**: Mark3Labs MCP SDK for Go — provides server framework, tool registration, and protocol handling
- **modernc.org/sqlite v1.46.1**: Pure Go SQLite implementation — enables zero-CGO static builds
- **GitHub Releases API**: For self-update mechanism (checking latest version, downloading binaries)
- **GoReleaser**: Build and release automation (triggered by git tag push)
- **golangci-lint**: Static analysis in CI pipeline
