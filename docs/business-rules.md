> ⚡ Auto-generated by sdd_reverse_engineer — review and refine as needed

# Hoofy — Business Rules

> Generated by [SDD-Hoffy](https://github.com/HendryAvila/Hoofy) | Stage 3: Business Rules
>
> Business rules are the DNA of your system. They define what is acceptable
> and what is not — across ALL processes, not tied to any single feature.
> (Source: Business Rules Manifesto, Ronald Ross, v2.0)

## Definitions (Ubiquitous Language)

> Terms that everyone in the project must use consistently.
> (Source: Domain-Driven Design, Eric Evans — Ubiquitous Language)

- **Observation**: A single memory entry representing a decision, pattern, bug fix, discovery, or other notable event from a coding session
- **Session**: A bounded period of AI-assisted development work, with start/end timestamps and an optional summary
- **Stage**: A discrete phase in the SDD pipeline that must be completed before the next stage can begin
- **Clarity Gate**: A quality checkpoint at the clarify stage that evaluates requirements across 8 weighted dimensions and blocks advancement until the score meets the mode threshold
- **Change**: A unit of ongoing development work (feature, fix, refactor, or enhancement) with an adaptive pipeline determined by type and size
- **Flow**: The ordered sequence of stages a change goes through, selected from the FlowRegistry based on change type × size
- **ADR (Architecture Decision Record)**: A document capturing an important architectural decision with context, rationale, alternatives rejected, and status
- **Principles**: Golden invariants — project rules that must NEVER be violated, established before requirements
- **Charter**: An enterprise-grade project definition capturing problem statement, target users, vision, boundaries, stakeholders, and constraints
- **Business Rule**: A declarative domain rule extracted from requirements using BRG taxonomy (definition, fact, constraint, or derivation)
- **Artifact**: A markdown file produced by a pipeline stage, stored in docs/ (project pipeline) or docs/changes/<slug>/ (change pipeline)
- **Relation**: A typed directional edge between two observations (depends_on, caused_by, implements, supersedes, relates_to, part_of)
- **Namespace**: A scoping mechanism for observations that enables memory isolation between sub-agents
- **Topic Key**: A stable identifier for upsert semantics — observations with the same topic key update rather than duplicate
- **Context Check**: A mandatory pre-work scan of existing specs, completed changes, and memory for conflicts with the proposed change
- **Wave**: A group of tasks that can execute in parallel based on dependency graph analysis
- **Composition Root**: The server.go file that wires all concrete implementations to their interfaces (DIP)

## Facts

> Relationships between terms that are always true.
> (Source: BRG Taxonomy — structural assertions)

- A Session has zero or more Observations
- An Observation belongs to exactly one Session
- An Observation may have zero or more outgoing Relations
- An Observation may have zero or more incoming Relations
- A Relation connects exactly two Observations directionally (from → to)
- A ProjectConfig has exactly one active Stage at any time
- A ProjectConfig has exactly one Mode (guided or expert)
- The StageOrder defines exactly 9 stages in a fixed sequence
- A Change has exactly one ChangeType and one ChangeSize
- A Change has exactly one Flow determined by its type and size
- The FlowRegistry defines exactly 12 flow variants (4 types × 3 sizes)
- Each flow ends with StageVerify
- StageContextCheck is always at index 1 in every change flow
- A project can have at most one active Change at a time
- A completed Change is archived from docs/changes/<slug>/ to docs/history/<slug>/
- An ADR is stored in docs/adrs/ with sequential NNN-slug.md naming
- The MCP server exposes 35 tools, 6 prompts, and 1 resource
- The Clarity Gate evaluates exactly 8 weighted dimensions

## Constraints

> Behavioral boundaries. Format: When <condition> Then <imposition> [Otherwise <consequence>]
> (Source: Business Rules Manifesto, BRG — action assertions)

- When the Clarity Gate score is below the mode threshold, Then the pipeline MUST NOT advance past the clarify stage
- When mode is guided, Then the Clarity Gate threshold is 70/100
- When mode is expert, Then the Clarity Gate threshold is 50/100
- When a stage has status "pending", Then only the immediately preceding stage may have status "completed" for advancement
- When a Change is created, Then no other active Change may exist (one active change at a time)
- When FTS5 search input contains special characters, Then input MUST be sanitized before passing to MATCH queries
- When building the binary, Then CGO_ENABLED MUST be 0 for static compilation
- When writing user-facing messages, Then output MUST go to stderr (stdout reserved for MCP stdio transport)
- When wrapping errors, Then MUST use fmt.Errorf("context: %w", err) for error chain preservation
- When sdd_init_project generates agent instructions, Then it MUST be idempotent (check for existing SDD header before writing)
- When creating an ADR, Then the nextADRNumber() function MUST handle gaps in the numbering sequence
- When a tool receives content, Then the AI generates the content, the tool only persists it (tools are storage, not generators)
- When resolving the docs directory, Then findProjectRoot() MUST walk up directories looking for docs/hoofy.json with docs/specs/hoofy.json as fallback


## Derivations

> Knowledge computed or inferred from facts and constraints.
> (Source: BRG Taxonomy — derivations)

- A Change is "small" when it requires 4 pipeline stages (3 content + verify)
- A Change is "medium" when it requires 5 pipeline stages (4 content + verify)
- A Change is "large" when it requires 6-7 pipeline stages (5-6 content + verify)
- The Clarity Gate score is derived as a weighted average of 8 dimension scores, where weights range from 5 to 10
- A Pipeline is "blocked" when the current stage is clarify AND the clarity score is below the mode threshold
- An Observation is "stale" when it exceeds the age threshold during mem_compact analysis
- A Topic Key is "stable" when it can be reused across sessions for upsert semantics without duplication



## Glossary

> Additional domain terms and abbreviations beyond the core definitions.

- **MCP**: Model Context Protocol — an open protocol for connecting AI assistants to external tools
- **SDD**: Spec-Driven Development — the methodology enforced by Hoofy's pipeline
- **FTS5**: Full-Text Search version 5 — SQLite's built-in full-text search engine
- **BRG**: Business Rules Group — taxonomy for classifying business rules (definitions, facts, constraints, derivations)
- **DDD**: Domain-Driven Design — approach using Ubiquitous Language for domain modeling
- **WAL**: Write-Ahead Logging — SQLite journal mode enabling concurrent reads during writes
- **DIP**: Dependency Inversion Principle — depend on abstractions, not concretions
- **SRP**: Single Responsibility Principle — one reason to change per module
- **OCP**: Open-Closed Principle — open for extension, closed for modification
- **GoReleaser**: Build automation tool for Go projects that creates cross-platform binaries
- **stdio**: Standard input/output — the transport mechanism for MCP communication
- **Composition Root**: The single place where all dependencies are resolved and injected
